{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && ls -la dist/"
  },
  "deploy": {
    "startCommand": "npm run serve:simple",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
